// ═══════════════════════════════════════════════════════════
// guideHand.js — 파라미터로 조종하는 벡터 손 그리기
// [가이드 단계 구현 예정] 명세서 §2.
// drawGuideHand(ctx, params, view)  view: 'side' | 'front'
// params: { wristAngle, devAngle, curl[5], curlTip, spread, pinchGap }
// ═══════════════════════════════════════════════════════════

export function drawGuideHand(/* ctx, params, view */) {
  // TODO(가이드 단계): side 뷰(팔뚝+손, wristAngle) 먼저 → front 뷰 확장
}
